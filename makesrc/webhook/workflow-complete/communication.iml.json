{
    "respond": {
        "type": "json",
        "status": 200,
        "body": {
            "received": true
        }
    },
    "iterate": {
        "container": "{{[body.data]}}"
    },
    "output": {
        "weldDataId": "{{item.weldData.eid}}",
        "isComplete": "{{item.weldData.isComplete}}",
        "isTest": "{{item.weldData.isTest}}",
        "status": "{{item.weldData.status}}",
        "weldEid": "{{item.weldData.weld.eid}}",
        "weldName": "{{item.weldData.weld.name}}",
        "weldSlug": "{{item.weldData.weld.slug}}",
        "documents": "{{item.documents}}",
        "submissions": "{{item.weldData.submissions}}",
        "completedAt": "{{now}}"
    }
}
